name: Release

on:
  push:
    tags:
      - 'v*.*.*'
      - 'v*.*alpha'
      - 'v*.*beta'

jobs:
  create_release:
    name: Create release
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create_release.outputs.id }}
    steps:
      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          prerelease: false

  build:
    name: Rust build on ${{matrix.os}}
    needs: create_release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, windows-latest, macos-latest]
        include:
        - os: ubuntu-24.04
          install-deps: |
              cargo install cargo-deb
              sudo apt-get update
              sudo apt-get install libc6-dev-arm64-cross crossbuild-essential-arm64
              rustup target add aarch64-unknown-linux-gnu
          build-cmd: |
            RUSTFLAGS="-C linker=aarch64-linux-gnu-gcc" cargo deb --no-strip -p dnglab --target=aarch64-unknown-linux-gnu && cp target/aarch64-unknown-linux-gnu/release/dnglab target/aarch64-unknown-linux-gnu/release/dnglab_linux_aarch64
            cargo deb -p dnglab && cp target/release/dnglab target/release/dnglab_linux_x64
          package-files: |
              target/debian/dnglab*.deb
              target/release/dnglab_linux_x64
              target/aarch64-unknown-linux-gnu/release/dnglab_linux_aarch64

        - os: windows-latest
          build-cmd: cargo build --release
          pkg-cmd: cd target/release/ && 7z a dnglab-win-x64_${GITHUB_REF_NAME}.zip dnglab.exe
          package-files: target/release/dnglab*.zip

        - os: macos-latest
          build-cmd: cargo build --release
          pkg-cmd: cd target/release/ && 7z a dnglab-macos-arm64_${GITHUB_REF_NAME}.zip dnglab
          package-files: target/release/dnglab*.zip

    steps:
    - uses: actions/checkout@v7
    - uses: dtolnay/rust-toolchain@stable
    - uses: actions/cache@v6
      with:
        path: deps/dist
        key: ${{ runner.os }}-deps-${{ hashFiles('deps/*') }}
    - uses: actions/cache@v6
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('Cargo.lock') }}
    - name: Download deps
      run: ${{ matrix.install-deps }}
      shell: bash
    - name: Build
      run: ${{ matrix.build-cmd }}
      shell: bash
    - name: Debug
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: debug
        path: .
    - name: Package
      run: ${{ matrix.pkg-cmd }}
      shell: bash
    - name: Publish
      uses: softprops/action-gh-release@v3
      with:
          files: |
            ${{ matrix.package-files }}
          prerelease: false
          draft: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
